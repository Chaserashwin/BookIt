{
  "version": 2,
  "builds": [
    { "src": "server/api/app.js", "use": "@vercel/node" },
    { "src": "client/vite.config.js", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/api/app.js" },
    { "src": "/(.*)", "dest": "client/dist/$1" }
  ]
}
